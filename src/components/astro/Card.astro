---
import clsx from "clsx";

const { class: className } = Astro.props;
---

<article
  class={clsx(
    "bg-gray-100 dark:bg-dark-600 p-2 rounded-sm hover:scale-[1.01] transition-all duration-300",
    className,
  )}
>
  <slot />
</article>
